# markdownlint configuration
default: true

# Allow long lines (ruff handles Python; docs can have long URLs)
MD013: false

# Allow duplicate headings in different sections
MD024:
  siblings_only: true

# Allow raw HTML in markdown (used for badges etc.)
MD033: false

# Allow first line to not be a top-level heading
MD041: false
